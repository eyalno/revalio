name: CI Demo

on:
  push:
    branches: ["main"]

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Print a message
      run: echo "GitHub Actions is running your workflow!"

    - name: Show current directory
      run: pwd

    - name: List files
      run: ls -la
